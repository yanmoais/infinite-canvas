---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

跨 Story、Pose、Character Atelier、Outpaint 和运行时能力的唯一总控计划：

- [Infinite Canvas 统一设计与开发计划 v1.0](../../../infinite-canvas-unified-development-plan.md)

## 当前批次：Gate 0 / C0 收敛

1. **C0-1 冻结边界**：记录主工作区、PoC 工作树与 Mission_manager Gateway 的 diff；停止交叉文件并行写；为两套前端固定不同端口或浏览器 profile。
2. **C0-2 Gateway 纠偏**：修正 Outpaint 失败 profile 的状态与过度因果备注；前端过滤 `enabled:false` profile；冻结实验使用的 Registry、Workflow 和 Gateway 版本。
3. **C0-3 证据合同**：Gateway receipt 增加 `actualLoras` 与 completeness，或建立经测试的完整列举不变量。
4. **C0-4 Shared Core 合流**：以主线为底逐文件合并类型、ExecutionPlan、source provenance、Outpaint operation preflight 和资产 ID 规范化。
5. **C0-5 UI 调用链合流**：合并续接模型选择、source lock、跨模型显式空 LoRA，同时保留主线 PromptCompiler、stale 传播和现有画布行为。
6. **C0-6 测试与文档**：合并双方测试与专项文档，同步总控、TODO、pending-test 和 CHANGELOG。
7. **C0-Z 独立验收**：运行约定验证并完成独立 review；关闭 Blocking/P0/P1 后，Gate 0 才能转为 `DONE`。

## 后续入口

- **Gate A / U1 Outpaint**：Gate 0 完成后冻结 U1-R0 manifest，补 QA pair schema 与实际 LoRA 完整性证明，再提交首轮 18 cells；之后继续 U1-R 和 U1-Z。
- **Gate B / U2-E Windows smoke**：同样等待 Gate 0；Gate 0 完成后可在 Gate A 等待 Windows/GPU 时并行，只做已实现编辑链的真实验证，不扩张产品范围。
- **Gate C→G**：自动引用 → Pose Runtime → Pose MVP → Character Atelier → Story P0，严格按总控前置推进。

已完成但等待用户验证的功能记录在[待测试](/docs/progress/pending-test)。未实现计划不得写入待测试。
