---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

跨 Story、Pose、Character Atelier、Outpaint 和运行时能力的唯一总控计划：

- [Infinite Canvas 统一设计与开发计划 v1.0](../../../infinite-canvas-unified-development-plan.md)

## 当前批次：Gate 0 / C0 收敛

1. **C0-1 冻结输入边界（✅）**：双工作树、Mission_manager、8080 进程、共享文件指纹和浏览器隔离边界已记录在 [`gate-0-convergence-snapshot.md`](../../../gate-0-convergence-snapshot.md)。BOSS 已确认继续完成 v0.9.0 merge；merge commit `62738dd2` 已创建，`MERGE_HEAD` 已清除，处置后 status 快照已落盘。
2. **C0-2 Gateway 纠偏（✅）**：失败 profile notes 已收敛为请求级观察；前端过滤 `enabled:false` profile；Gateway 候选拒绝显式 disabled ID。候选基线、静态检查与独立 review 已通过，见 [`gate-0-c0-2-candidate-baseline.md`](../../../gate-0-c0-2-candidate-baseline.md)；C0-2 当时未重启共享 8080，后续加载与冻结状态以 C0-3 证据为准。
3. **C0-3 证据合同与最终冻结（✅）**：receipt v2 已由共享 Gateway 加载；bare/no `lora_keys`、动态 `LoraLoader`、build-workflow 前真实失败与 invalid-preset planned/actual/`fallback.preset` 四类在线合同均通过。HTTP/持久化一致性以 [`.uat/gate-0/c0-3-receipt-v2/evidence-manifest.json`](../../../../.uat/gate-0/c0-3-receipt-v2/evidence-manifest.json) 可独立复算。Registry `a0ff5c…`、Gateway `83a767…` 与 71-workflow 集合 `c0c4e4…` 命中冻结值。FaceID opaque/incomplete、多阶段 live 留 Gate B。证据见 [`gate-0-c0-3-candidate-baseline.md`](../../../gate-0-c0-3-candidate-baseline.md)。
4. **C0-4 Shared Core 合流（✅）**：以主线为底逐文件合并类型、ExecutionPlan、source provenance、Outpaint operation preflight 和资产 ID 规范化；复审 P0/P1 已关闭（中文诊断后缀 + 不可执行 binding 不写入 plan）。未改动任何 UI 调用链。证据见 [`gate-0-c0-4-shared-core-merge.md`](../../../gate-0-c0-4-shared-core-merge.md)。
5. **C0-5 UI 调用链合流（✅）**：Outpaint 对话框接入续接模型选择；`project.tsx` 接 source lock、`preflightComfyOutpaint`、跨模型 `modelOverride`/显式空 LoRA，并在主线提示词编译后写入 Shared ExecutionPlan。保留默认词/用户直译路径与 stale 传播。证据见 [`gate-0-c0-5-ui-callchain-merge.md`](../../../gate-0-c0-5-ui-callchain-merge.md)。
6. **C0-6 测试与文档（✅）**：U1-R0 完整执行合同与 schema 已落盘（`docs/u1-r0-execution-contract.md`、`.uat/u1-outpaint/u1-r0/`）；离线合同测试 `web/tests/u1-r0-contract.test.ts` 通过；PoC Fooocus recipe 测试明确不合入产品路径。真实实验 `manifest.json` 仍未冻结。证据见 [`gate-0-c0-6-tests-and-docs.md`](../../../gate-0-c0-6-tests-and-docs.md)。
7. **C0-Z 独立验收（✅）**：约定测试 7/7 PASS；diff check 确认 C0-4/C0-5 合流语义在位；多轮独立 review 关闭全部 Blocking/P0/P1（终轮 PASS）。证据见 [`gate-0-c0-z-acceptance.md`](../../../gate-0-c0-z-acceptance.md)。Gate 0 = `DONE`。

## 后续入口

- **Gate A / U1 Outpaint（可开工前置仍缺真实 manifest）**：Gate 0 已 DONE；下一步基于 C0-6 合同生成并冻结真实 U1-R0 `manifest.json`（填齐资产 SHA + 通过 `validateFrozenManifestSemantics`），再提交首轮 18 cells；之后完成 U1-R 和 U1-Z。
- **Gate B→G**：必须等待 Gate A 取得 `QUALIFIED`、`PARTIAL` 或 `REJECTED` 合法出口；随后按 Windows edit smoke → 自动引用 → Pose Runtime → Pose MVP → Character Atelier → Story P0 严格推进。

已实现但等待用户验证的功能记录在[待测试](/docs/progress/pending-test)。未实现计划不得写入待测试。
